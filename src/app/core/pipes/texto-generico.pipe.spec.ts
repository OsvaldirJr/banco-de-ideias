import { TextoGenericoPipe } from './texto-generico.pipe';

describe('TextoGenericoPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoGenericoPipe();
    expect(pipe).toBeTruthy();
  });
});
